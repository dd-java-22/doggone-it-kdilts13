Change by Codex: Add model init POC + remove litert-support

Add a minimal ScanAnalysisFragment trigger that loads breed labels + model assets and logs input/output tensor metadata via a small helper.
Remove litert-support dependency to resolve duplicate TensorFlow Lite class conflicts with litert runtime.

Prompt: Please implement the smallest possible proof-of-concept setup for on-device dog-breed classification in the existing Android project.

Important constraints:
- Do NOT implement the full image classification flow yet.
- Do NOT add camera or gallery code.
- Do NOT add UI beyond the smallest temporary button/text needed on ScanAnalysisFragment.
- Keep the code simple, easy to explain, and easy to debug.

Project context:
- The app already has ScanAnalysisFragment.
- The following files already exist in app/src/main/assets/:
  - breed_model.tflite
  - breed_names.json
- A bundled test dog image already exists in res/drawable as a static drawable resource.
- Gradle already includes the LiteRT runtime/support dependencies and noCompress for tflite.

What I want in this step:
1. Add the smallest temporary trigger on ScanAnalysisFragment, such as a button labeled "Init Model POC".
2. When the button is clicked, do the following:
   - load breed_names.json from assets
   - parse it into a usable in-memory list of breed names
   - load breed_model.tflite from assets
   - initialize a LiteRT/TFLite interpreter
   - inspect and log the model input tensor details
   - inspect and log the model output tensor details
   - log the label count from breed_names.json
3. Log enough detail to verify setup, including:
   - input tensor shape
   - input tensor data type
   - output tensor shape
   - output tensor data type
   - number of labels loaded
4. Keep the implementation modular:
   - put model/label loading and tensor inspection into a small helper/service class rather than stuffing everything directly into the fragment
   - keep the fragment thin
5. Use the existing project language/style conventions.
6. Handle errors cleanly:
   - if assets fail to load or parsing fails, log a clear error instead of crashing
7. Do not overengineer:
   - no ViewModel unless truly needed
   - no repository layer for this
   - no fake production architecture
   - no custom back-stack/navigation changes

Also:
- clean up imports
- keep naming conventional
- summarize what files you created/modified
- explain how I should manually verify success in Logcat

Follow-up:
Use option 2. Remove the LiteRT support-library dependency for now and keep only the LiteRT runtime dependency needed for the current POC.
